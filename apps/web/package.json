{
  "name": "@potg/web",
  "version": "0.2.0",
  "private": true,
  "description": "PropertyOnTheGo web app — app shell (auth, account switcher, nav), the property portfolio screens, and the reusable Ask AI panel wired to POST /ai/actions + POST /ai/chat. Built from the published Ask AI panel wireframe's Main.dc.html artboard. See README's 'Web app (Foundation + Ask AI panel)' section.",
  "scripts": {
    "start:api": "npm run start --workspace=apps/api",
    "dev:api": "npm run start:dev --workspace=apps/api",
    "build:api": "npm run build --workspace=apps/api",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^1.46.0",
    "next": "^16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "typescript": "^5.5.4"
  }
}
